# App
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Test login (never enabled in Vercel production). Preview should keep credits at 0.
ENABLE_TEST_AUTH=
NEXT_PUBLIC_ENABLE_TEST_AUTH=
TEST_AUTH_CREDITS=0

# Database (Supabase Postgres)
# Use Pooler URL (usually port 6543) for `DATABASE_URL`.
DATABASE_URL=

# Kie AI (existing)
NANO_BANANA_API_KEY=
KIE_API_KEY=

# Vercel Blob
BLOB_READ_WRITE_TOKEN=

# Stripe — use test keys/prices outside production and live keys/prices only in production.
# Test and live Products/Prices are separate Stripe objects.
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
STRIPE_PRICE_STARTER_MONTHLY=
STRIPE_PRICE_STARTER_YEARLY=
STRIPE_PRICE_PRO_MONTHLY=
STRIPE_PRICE_PRO_YEARLY=
STRIPE_PRICE_MAX_MONTHLY=
STRIPE_PRICE_MAX_YEARLY=

# Vercel Cron / manual: GET /api/cron/monthly-credits with Authorization: Bearer <secret>
CRON_SECRET=

# Analytics
NEXT_PUBLIC_GA_MEASUREMENT_ID=
